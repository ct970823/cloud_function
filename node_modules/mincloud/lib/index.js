'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

require('./polyfill');

var _engine = require('./engine');

var _engine2 = _interopRequireDefault(_engine);

var _utils = require('./utils');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

exports.default = function () {
  return (0, _engine2.default)().then(function (e) {
    var cmd = e.config.get('cmd');
    if (cmd && e.cli[cmd] != null) {
      var _e$cli;

      return (_e$cli = e.cli)[cmd].apply(_e$cli, _toConsumableArray(e.config.get('params')));
    } else {
      console.log('用法：');
      console.log('    ' + e.config.get('prefix') + ' <command>');
      console.log('');
      console.log('支持的 command 有：');
      console.log('    ' + Object.keys(e.cli).join(', '));
      console.log('');
      console.log('- ' + e.config.get('prefix') + ': v' + e.config.get('version'));
      console.log('- node: ' + process.version);
    }
  }).catch(_utils.errorHandler);
};